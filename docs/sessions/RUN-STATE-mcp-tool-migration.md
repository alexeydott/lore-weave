# RUN-STATE — MCP tool-catalog migration close-out

**Purpose:** the open-items ledger for the tool-unification effort, so nothing drifts across
compaction. **Re-read this file first after any compaction, before `git log`.**
Branch `feat/frontend-tools-mcp-migration`. Opened 2026-07-23.

**Rule:** an item is `DONE` only with an **evidence string** (a measured number, a DB row, a
pasted output). "Looks right" / "tests pass" is not evidence for a cross-service claim — that
is exactly what let `Items any` de-federate 54 tools with every gate green.

---

## Slice board

| # | Slice | Status | Evidence |
|---|---|---|---|
| S1 | Live-smoke the 5 KG merges (`kg_build`, `kg_graph_query`, `kg_ontology_propose`, `kg_view_edit`, `kg_add_nodes`) | **DONE** | 12 branches through ai-gateway: **9 executed w/ real effect, 2 correct preconditions, 1 unreachable-on-fixture, 0 defects**. `kg_view_edit` upsert→delete round-trip; `kg_add_nodes from_glossary` = `nodes_created:16`. `docs/eval/tool-liveness/kg-unification/LIVE-SMOKE.md` |
| S2 | Dedup identical duplicate tool calls | **DONE (partial, honest)** | Split into TWO defects. **S2a** same-pass collapse: 6 unit tests, ⚠️ not yet live-exercised. **S2b** `created:0` read as failure: retries **3→2**, user now told *"Lâm Uyên is already in the glossary"* instead of *"I can't save that"*. `docs/eval/tool-liveness/duplicate-tool-calls/RESULTS.md` |
| S4 | **Depth-first: clear the red baseline** (old bugs corrupt every new measurement) | **DONE** | **25 → 0. chat-service 1860 pass / 0 fail.** 4 real product bugs found + fixed (browser-executed conflation ×4 consumers, incl. a subagent tool-scope leak). Runtime-verified + E2E (`max_consec` 3→1). |
| S5 | **Pay the coverage debt** — kill-switches untested in either state | **7 of 8 PAID + GATED** | Audited 15 boolean flags: **8 had zero coverage**. Now covered both-state: `compact_studio_panel_desc`, `studio_panel_intent_gated`, `compact_task_elastic_enabled`, `compact_breadcrumb_enabled` (mutation-proven), `compact_recovery_hint_enabled`, `compact_persist_enabled`, `lazy_workflow_directive`, **`rail_driver_enabled` (mutation-proven)**. Debt down 7 → **1** row (`minio_use_ssl` — infra transport, no chat-service path to assert). A **gate** fails any NEW untested flag (proven). |
| S5b | **`rail_driver_enabled`** — the row that was *sharpened instead of paid* | **PAID** | The first attempt stopped because mocking 4 of the rail's 5 preconditions still yielded 0 probe calls, which would have made the OFF assertion **vacuous**. `tests/test_rail_flag_kill_switch.py` supplies all five (book_context UUID · agui + tool-calling · a mode binding that pins `vision-to-book` · that slug visible · grant OWNER) and asserts the **ON case first** (`probe calls == 1`) so the OFF cases cannot pass for the wrong reason. Effect asserted on the artifact the lever controls — the **system prompt shrinks** when OFF, not just the spy. Mutation-verified: deleting `and settings.rail_driver_enabled` from `stream_service.py:4545` reds exactly 2 tests; restored → green. A 4th test pins that OFF is a **revert path, not a failure mode** (the turn still completes). Bonus: 3 tests pin the *other* preconditions independently, including the /review-impl HIGH — a caller with **no grant is never probed** (client-supplied `book_id` fanning out to routes that don't grant-check). **That last one caught a bug in my own fixture**: `level or DEFAULT` silently upgraded `GrantLevel.NONE` to OWNER because IntEnum(0) is falsy — the same probe-bug-changes-the-verdict class as K13/K14. **33 pass** across the three kill-switch suites. |
| S3 | composition-service unification (~89 tools) | DEFERRED — do after S5 debt burn-down | — |

### Closed earlier this session (evidence recorded)
- Glossary de-federation (`Items any`) — FIXED + gated. `235 PARTIAL → 289 tools`; `0 → 54` glossary_*. `3e6df3a56`
- Federation-safety guard on all 10 providers — SDK panic (Go×5) + shared checker (Py×5). Live scan `0` real violations. `4d6f5e2fd`
- Outage visibility F1–F4 — agent now says "temporarily unavailable" instead of blaming the user; `/health/federation` 200→200→**503**→200. `4abda32b7`
- Placeholder-id silent no-op + skill drift — 13→3 calls; entity `019f8cbe-…` DB-verified. `6ba95f4ef`

---

## Known-open register (NOT drift — deliberately parked, with the reason)

| ID | Item | Why parked |
|---|---|---|
| ~~K1~~ | **CLEARED — 25 → 0** (chat-service 1886 pass / 0 fail). Was mis-parked as "different track"; it was the measurement-corrupting class. |
| ~~K2~~ | **RESOLVED + it was bigger than recorded.** Not one tool: ALL THREE `*_task_provide_input` (book/composition/glossary) carried NO tier, and `tool_tier()` defaults missing→"R" (inert), so all three **passed the ask-mode read-only filter** — a read-only turn could drive a pending gate to completion, i.e. perform the write ask mode exists to withhold. Fixed in BOTH kits (tier A, scope user) + guards. Live-verified: ask-mode survivors 110→107, zero `provide_input` remaining. Also closed a kit-parity gap (Python registration had no description/synonyms while Go did) which was reddening 3 composition unit tests. |
| ~~K3~~ | **CLEARED 2026-07-23.** All four rebuilt + redeployed with the guard compiled in. A boolean subschema would PANIC the service at boot, so a clean boot is the proof: `book/catalog/agent-registry/provider-registry` all `Up (healthy)`, zero panic lines in any log. ai-gateway re-federated: **10/10 providers available, `partial:false`, `unavailableProviders:[]`, 299 tools**. |
| K4 | Envelope long-tail `ambient_book`/`ambient_project` tagging | Parked by user decision — do at schema-drop time. |
| K5 | A real unified cross-store search | **Never-built feature, correctly deferred — NOT a bug.** No tool exists that fans across manuscript + glossary + KG + motifs in one call; each store has its own search (`story_search`/`book_search`/`glossary_search`/`memory_search`/`composition_motif_search`). Crucially nothing ADVERTISES cross-store search and under-delivers, so there is no silent seam — just an unbuilt capability, out of scope for a catalog-SHAPE migration. (The over-claim that was tangled in this row — `story_search` billing itself "the universal find tool" — was split out as **K26** and FIXED; that part WAS a bug.) |
| ~~K26~~ | **`story_search` over-claimed cross-store reach — FIXED (split out of K5 when the user pushed on the classification).** It searches MANUSCRIPT PROSE only but called itself "the universal find tool" — so a model would search for a character, get prose hits, and conclude it had searched everything, never calling `glossary_search`/`memory_search` (a false negative — the K16/K20/K22/K23 advertise-reach-you-lack class, in prose). Intent was ALWAYS "universal MANUSCRIPT search" (the sibling tests say so; "manuscript" had dropped out of the prose), so this restored documented scope + added an explicit redirect to the sibling stores. Catalog-wide sweep confirmed it's the SOLE genuine scope over-claim (`kg_world_query`'s "across all books in a world" is accurate) → focused regression assertion, not a noisy gate. Mutation-verified; LIVE description scopes to prose + names both siblings. |
| ~~K6~~ | **RESOLVED live — and "blocked" was the wrong word.** Both ops need exactly ONE thing: an ADOPTED project-scoped schema. That is a two-call setup, not a dependency — `op=adopt_template` mints a confirm-token and `POST /v1/kg/actions/confirm` redeems it, and that route has an **internal-token replay mode**, so the claim that it "needs the GUI review surface" was false. (CLAUDE.md: missing infrastructure is unbuilt work, not blocked — this row is the exact lazy-tell that rule exists to kill.) Executed on a throwaway project: BEFORE adoption both ops refuse with actionable messages (*"no adopted ontology to edit — adopt a project schema first"* / *"…to sync — adopt a template first"*) — correct validation, never a silent no-op; AFTER adoption **`op=schema_edit` mints** (`add edge_type 'k6_probe_edge' (schema v1 → v2 on confirm)`) and **`op=sync_apply` mints** (`0 take-theirs / 1 keep-mine`), with `base_source_hash` taken from `kg_sync_available`'s real `source_ref`. Probe project + adopted schema deleted, scoped to their own ids. |
| K8 | One `propose_entities` retry survives the SUCCESS guidance (2 calls, was 3) | **Won't-fix UPHELD on re-audit 2026-07-23, but the original reason was weak and the row was scoped too narrowly.** Verified: (1) no data harm — the path is idempotent (`skipped_exists`); (2) a CROSS-ITERATION backstop already exists — `TIER_A_SAME_OP_CAP=5` per TURN, and this tool is tier A, so a runaway repeat escalates to a human confirm card; (3) the house pattern for repeat-writes is **tool-level idempotency at the domain** (`book_chapter_create` N6, from dogfood F7 "the agent double-firing the tool in one turn can't leave a duplicate chapter") — a loop-level suppressor would duplicate a mechanism the architecture deliberately puts in the domain; (4) the narrow fix I dismissed too fast (honour a machine-readable "a byte-identical repeat is a no-op" marker) needs a NEW cross-service output contract ⇒ defer-gate #2 (large/structural), not a quick edit. |
| K13 | **SETTLED. 9 confirmed duplicating → 9 fixed; final re-probe `DUPLICATING: none`.** Fixed: `book_create`, `world_create`, `kg_project_create`, `composition_arc_create`, `world_map_create`, `world_map_add_marker`, `world_map_add_region`, `composition_canon_rule_create`, `composition_outline_node_create`. Confirmed ALREADY SAFE (no change needed): `composition_create_work` (idempotent), `composition_motif_create` + `composition_structure_template_create` (return `outcome: applied_conflict`, no row), `book_chapter_create` (N6), `glossary_propose_entities` (`skipped_exists`), `glossary_create_chapter_link` (PG 23505), `glossary_create_evidence` (`uq_evidence_dedup`). Guards are N6-shaped: pre-insert lookup on the natural key, live rows only, scoped so legit same-name siblings under a different parent still create; no DB uniques added. **THREE harness bugs changed verdicts mid-audit** — echoed input ids faked an IDEMPOTENT pass, positional id-picking invented K14, and reading only `isError` hid two SAFE conflict-rejections as 'inconclusive'. **ROUND 3 (2026-07-23) — the last 8 settled; a 10th duplicator found.** **`book_chapter_bulk_create` DUPLICATES → FIXED.** Its own description promises "Idempotent on original_filename", and that held only for callers who SUPPLY one. Omitted, the key is auto-generated as `chapter-%04d.txt` from `sortOrder` = `MAX(sort_order)+1` — so call 1 minted `chapter-0002.txt`, call 2 `chapter-0003.txt`, and the dedup set could never match. **The key was derived from the very state the previous call had just changed**, making the documented idempotency vacuous *by construction*, and vacuous exactly where it matters: a folder import carries real filenames, an AGENT creating chapters from chat carries none. Fixed with the N6 natural key (title+language, non-empty titles only) when no filename is given; explicit filenames keep the original behaviour, so folder import (same title across volumes) is untouched. 3 DB-gated tests, mutation-verified (removing the guard reds exactly the 2 duplicate cases, leaves the filename-import case green); LIVE after rebuild: repeat ⇒ `created:0 skipped:1`, in-batch dup ⇒ `created:1 skipped:1`. **Confirmed SAFE by real DB uniques, correctly tenant-scoped:** `glossary_book_create` / `glossary_user_create` (`UNIQUE(book_id,code)` / `UNIQUE(owner_user_id,code)`), `composition_scene_link_create` (`UNIQUE(from,to,kind)`), `composition_motif_link_create` (same), `composition_entity_override_add` (`UNIQUE(work_id,target_entity_id)`), `kg_create_node` (Neo4j MERGE). Their agent-facing REJECT text was checked too (the S2b half): `glossary_book_create` names the remedy tool (*"…use glossary_book_patch to edit it"*), `glossary_user_create` does not — a small asymmetry, not a defect. `kg_add_nodes` args were mis-shaped by the probe, not the tool. |
| ~~K14~~ | **NOT A PRODUCT BUG — my probe's error, retracted.** `world_map_create` was right to answer "world not found": the gateway serialises object keys ALPHABETICALLY, so `world_create`'s detail returns `bible_book_id` BEFORE `world_id`, and my positional `ids_of()[0]` handed a BOOK id to the map tool. Fixed the probe to name the key. Recorded because I logged it as a possible product defect on 3/3 reproductions — the reproduction was of my own harness. |
| ~~K10~~ | **RESOLVED — and the row was pointed at the wrong risk.** "Advertised locally but executes via ai-gateway" is low-severity (ai-gateway is a hard dependency anyway). The REAL exposure was the second copy of the schema, and a live diff settled it: the ARG SCHEMA is byte-identical (because `TestResidualAdvertisedDefsMatchContract` pins that slice against `contracts/frontend-tools.contract.json`) but the **DESCRIPTION had already drifted** — chat said "the user's current selection", ai-gateway says "the current selection". Seven characters, semantically identical, and exactly the proof: the pinned field held, the unpinned field did not. A description decides WHEN the model reaches for a tool, so it is a one-name-two-behaviours surface, not a typo. Re-synced to ai-gateway (the owner since P2.2, whose source calls the prose "a MOVE, not a duplication — Phase 4 removes chat-service's copy") + a drift guard reading the TS source directly, mutation-verified. Phase 4 still deletes the copy and retires the guard. |
| ~~K11~~ | **RESOLVED — and it was NOT flaky, NOT an xdist issue, and NOT a test bug.** It failed deterministically in the FULL suite (serial too) and passed alone, because `loreweave_obs.RedactFilter` corrupted any log call carrying a single dict arg: stdlib unwraps `logger.x(msg, a_dict)` to `record.args = a_dict`, and iterating a Mapping yields KEYS — so `tuple(...)` rewrote `{"event_type":…,"payload":…}` into `("event_type","payload")`, the message formatted one `%s` against a 2-tuple, and logging raised inside emit. The stdlib swallows that via handleError, so **the log line was silently LOST** — in every Python service using the kit. Redaction also never touched mapping VALUES, where a secret would actually sit. Fixed + 4 guards. composition 2300 pass / 0 fail; shared SDK suite 10 → 7 failures. **DEPLOYED + live-verified 2026-07-23** across chat/composition/knowledge/jobs/lore-enrichment (source-fixed alone was NOT cleared — every running image still carried the log-losing filter until rebuilt). |
| K15 | **Hardcoded absolute HOST paths in committed code — 8 sites, FIXED + GATED.** Surfaced by the user pushing back on my "host-env drift, not repo debt" call for K12's 7th failure. That call was right about the `.pth`, but wrong to stop there: a repo-wide sweep found 8 real ones — 4 POC exporters rooted at a DIFFERENT checkout (`lore-weave-mvp`), `add_mining_i18n.py` rewriting i18n through `d:/Works/source/lore-weave/...`, `eval_a_fair.py` sys.path-inserting an absolute path, and 2 external-corpus paths. In-repo targets now derive from `__file__`; external ones are env-overridable AND fail loudly. New pre-commit gate `scripts/no-absolute-host-paths.py` (verified: reds on a planted violation, clean after), registered in `docs/standards/README.md`. |
| K12 | **6 of 7 were real repo debt → FIXED. The 7th is NOT repo debt.** Fixed: (a) the prompt-family taxonomy split on a NAME SUFFIX (`*_system`), so the third family `summarize_level` ({level}/{child_texts}/{entity_names}, no {text}) was filed as text-bearing and reddened 3 tests for a correct prompt — families are now DERIVED from the placeholders a template actually declares, plus a test that every prompt lands in exactly one family and one that every prompt fully substitutes its own placeholders; (b) the `event` backward-compat hash, stale since `030429658` deliberately edited `event_extraction_system.md` — only `event` drifted and only that file changed, so re-pinning is correct and the guard did its job by forcing a conscious act; (c) `test_filter_enabled_without_model_ref_raises`, which asserted pre-`D-WX-PRECISION-FILTER-MODEL-ARCH` behaviour — enabling the filter now FALLS BACK to the extraction model (the env/global one was cross-tenant and 404'd); split into a fallback test + a still-raises-with-no-model-anywhere test. **NOT repo debt:** `test_video_gen::…regression_lock` fails only in MY environment — an editable install (`__editable__.loreweave_llm-0.1.0.pth`) points `loreweave_llm` at a SIBLING CHECKOUT (`lore-weave-mvp`), so two copies of the module load and `isinstance(LLMVideoContentPolicy(...), LLMVideoContentPolicy)` is False. Containers resolve it from site-packages and are unaffected. Host-env drift, not a code defect — deliberately NOT 'fixed'. |
| ~~K9~~ | **RESOLVED — by injecting the duplicate instead of waiting for it.** Two post-fix live runs failed to reproduce the emission (the model simply behaved), so "run it again and hope" was never a verification strategy. The real gap was sharper than the row said: all 6 existing tests call `_collapse_identical_tool_calls` **directly**, so NOTHING covered the wiring at `stream_service.py:2143` — a correct helper nobody calls is this repo's recurring silent-no-op shape. Added `TestCollapseIsWiredIntoTheLoop`: drives the real `_stream_with_tools` with a gateway script emitting the exact measured shape (3 byte-identical calls, iteration 0, streamed id+name-then-args like the gateway really streams) and asserts on EFFECT — the tool executes **once**; the collapsed ids never reach the provider (every advertised `tool_call` id has a matching result, else the provider waits forever); and a genuine 2-different-args batch still runs **both** (the anti-over-collapse guard, without which the first test would also pass for a loop that dropped everything after the first call). **Mutation-verified, and the mutation is the finding:** deleting the collapse call from the loop leaves all 6 pre-existing helper tests GREEN and reds only the new ones. |
| ~~K7~~ | **RESOLVED live — and walking the remedy chain uncovered TWO real defects.** The row was a fixture gap, not a blocker: the remedy the error names is itself agent-callable. `kg_build target=graph` executes (real 563-char class-C confirm_token, `descriptor=kg_build_graph`, nothing spent). On the way, D-EMB-MODEL-REF-04 turned out to be defeated **from both ends**: **(A)** `_GRAPH_LABELS` omits `:Passage` — the node carrying the vector — so `PUT /embedding-model?confirm=true`, whose entire job is "delete the stale vectors first", deleted everything EXCEPT the vectors (its own comment claimed otherwise; proven by running that exact label loop over a synthetic Neo4j tenant, where the `:Passage` node was the only survivor). **(B)** three call sites read `extraction_status != 'disabled'` as "has a graph", but `POST /extraction/disable` sets `'disabled'` while **preserving** the graph (`graph_preserved: true`) — so a vector-full project read as empty and an AGENT could swap the model with no confirm and no purge. The column also cannot distinguish delete-graph from disable: both land on the same terminal `'disabled'`. A defeats the confirm path, B routes around it; fixing either alone leaves the hole open. New `project_has_embedded_passages` (fails CLOSED — a false "no vectors" destroys retrieval, a false "has vectors" only routes to the confirm path) + an `include_passages` flag, True at the two model-CHANGE sites only (delete-graph/rebuild keep passages ON PURPOSE: they do not change the vector space, and chat/glossary chunks are not rebuildable). Both mutation-verified; live: a project seeded with ONE `:Passage` while still `'disabled'` is now REFUSED. knowledge-service 4072 pass / 0 fail. |
| ~~K16~~ | **RESOLVED + live-verified. 299/299 tools now advertise ONE convention (was 250 flat / 49 wrapped).** Cause was a FastMCP signature detail, not a design choice: `async def t(ctx, args: Model)` publishes a single `args` wrapper + a `$ref`, `async def t(ctx, a: str, b: str)` publishes flat properties — two authoring styles in one file became two calling conventions on the wire, splitting SIBLING PAIRS (`*_create` wrapped, `*_delete` flat). Fixed at the shared kit chokepoint (`loreweave_mcp/flat_args.py`, alongside the existing `patch_convert_result`/`patch_tool_run_size_gate` precedent) rather than by editing 49 handler signatures — **the wire shape changes, Pydantic stays the single validation truth**. Two deliberately asymmetric halves: the SCHEMA is flattened (hoist properties/required, keep `$defs` so nested `$ref`s still resolve, preserve `enum` + `additionalProperties`), and CALLS accept BOTH shapes forever — a saved workflow or cached schema still sending `{"args":{…}}` must not break. Both halves mutation-verified independently. LIVE after rebuild: `composition_motif_link_create` schema is flat with its `kind` enum intact; a FLAT call now works (was `Field required: args`); the legacy WRAPPED call still works; **survey re-run: 299 flat / 0 wrapped, federation `partial:false`**. |
| ~~K17~~ | **RESOLVED + live-verified — and the `applied_conflict` carve-out is what makes it safe.** 60 composition sites returned `{"success": False}` as a NORMAL result, so `isError:false` made a failure indistinguishable from a success at the protocol level. Split measured before touching anything: **44 genuine failures, 16 `outcome: applied_conflict`** — and those 16 are idempotent no-ops K13 audited as CORRECT. Flagging them would have repeated S2b (`created: 0` read as failure → agent told the user *"I can't save that"*), so a payload that declares an `outcome` passes through untouched. Fixed at the kit chokepoint (`loreweave_mcp/error_signal.py`): a failure RAISES, so the SDK's own error path sets `isError` — the envelope, content shape and logging all stay the SDK's; we only decide WHEN it is an error. Error text is emitted **C4-shaped (`message`, not `error`)** so the first-party prose does not regress. LIVE: invalid edge ⇒ `isError:true` + the same sentence as before; duplicate motif ⇒ `isError:false` + `outcome: applied_conflict`. |
| K18 | **`_error_envelope`'s C4 decoding had NEVER fired in production — found while fixing K17.** `chat-service/app/client/knowledge_client.py` documents decoding `{"code"?, "message", "detail"?}` so a workflow can branch on a stable code (contract C5) instead of pattern-matching prose. But FastMCP wraps every raised `ToolError` as `Error executing tool <name>: {…}`, so `text.startswith("{")` was ALWAYS False and every C4 body degraded to raw text — the `code` was silently lost on every single tool failure, for knowledge-service too (it has raised since D-KNOWLEDGE-TOOL-ERRORS-NOT-ISERROR). It went unnoticed because the unit tests fed the function a BARE `{"message": …}` string — **the shape the contract describes, not the shape the producer emits** (the `feedback_test_input_fields_from_producer_schema` trap, in the repo's own code). Fixed with a deliberately narrow stripper (the regex requires a `{` right after the colon, so genuine prose starting with those words is untouched) + tests using a verbatim live capture. Mutation-verified. |

| ~~K19~~ | **61% of every advertised arg reached the model UNDOCUMENTED — 685/1132. Now 327 (29%).** The split was not by team or age but by **LANGUAGE**: Go's `jsonschema:"…"` tags land in the schema; Python's `Annotated[str, "mine \| system \| all"]` does not, because Pydantic honours only `Field`/`Doc` inside `Annotated` and keeps a bare string as opaque metadata. **composition and kg were at 100% undocumented.** The docs existed, were accurate, and were invisible to the only reader that matters. Nothing was lost — Pydantic keeps them on `FieldInfo.metadata` — so the kit promotes them at registration (`arg_docs.py`), additively (an existing description is never overwritten) and resolving the INNER model for K16-flattened tools, else the 49 tools needing docs most would have got none. **That alone did not move kg**, which exposed the real second defect: **knowledge-service built its own FastMCP and hand-imported ONE kit patch**, silently opting out of every other fix applied at the chokepoint — the same "second service missed it" shape as `closedSetSchemaFor`. Its construction was byte-identical to `make_stateless_fastmcp`, so it now goes through it. LIVE: kg **128/128 → 0/128**; translation/plan/jobs/memory/story → 0%; federation `partial:false`. composition's remaining 276 are args with **no documentation anywhere in the source** — real authoring debt, deliberately not papered over. |
| ~~K20~~ | **A closed set declared ONLY in prose — 4 args, 2 of them this migration's own discriminators.** `book_structure_edit.op` (`create_part \| rename_part \| …`) and `book_list.kind` (`books \| chapters \| …`) enumerated their legal values in the DESCRIPTION while advertising a bare string — the exact shape of the shipped `panel_id` bug (gemma sent `panel:"editor"`, resolver matched nothing, model reported success for work that never happened). For a dispatch discriminator a miss IS the failure mode. glossary already had `closedSetSchemaFor`; book-service did not — precisely how the second service ended up without it — so the helper moved to the kit (`lwmcp.ClosedSetSchema`) and glossary's copy delegates. **The generalisable half is the GATE**: `TestEveryEnumeratedClosedSetHasAnEnum` walks the real in-process `tools/list` and reds when a description enumerates a set the schema does not declare; mutation-verified by removing the `op` enum (reproduces the original bug and names the fix), and it **guards itself** — if the detector ever matches nothing catalog-wide it fails loudly instead of passing vacuously. LIVE: enum-in-prose **4 → 0**, enums on the wire. |
| — | **Audit honesty note.** The same sweep flagged 26 enum-less "closed-set-looking" args and 6 "DB-CHECK-proven" ones. After triage: **4 real**. The rest were my heuristics being crude — `tension_target` is an `int`; `glossary_propose_new_entity.kind` is a per-book ontology code (correctly NOT an enum); and the DB cross-reference unioned CHECK values **by column name across tables**, so `kind`/`status` blended every table's set into nonsense. Recorded because a sweep that reports its noise as findings is how a register fills with work that does not exist. Genuinely open: `composition_canon_rule_create.scope` (DB CHECK `world\|entity\|reveal_gate`, arg is a bare `str` with no description) and `composition_arc_template_list.scope`/`.status` (runtime-checked closed sets) — small, in composition, and now visible to the gate pattern. |

| ~~K21~~ | **ai-gateway's own 8 tools were the one surface exempt from the tier invariant — now declared + compiler-enforced.** Every domain service is FORCED to declare a C-TOOL tier (Go panics via `MustValidateToolMeta`, Python raises via `require_meta`), but ai-gateway's hand-written defs are covered by no service gate. `tool_list`/`tool_load` were fixed when a federated `tools/list` first showed untiered tools — the 7 `ui_*` and `propose_edit` were missed, and the audit still found exactly those 8. Why it matters in this repo's own words: `tool_tier()` defaults missing→`R`, which is the right SAFETY default but makes an *undeclared* tool indistinguishable from a deliberately-read-only one — the sharp version is K2, where three untiered `*_task_provide_input` tools passed the **ask-mode read-only filter** and a read-only turn could drive a pending gate to completion. **R is DECLARED, not changed** (zero behaviour delta): a `ui_*` call changes what the user is LOOKING AT, never what is stored, and the page loads via normal authenticated routes ⇒ `scope:'none'`; `propose_edit` commits nothing (human clicks Apply) ⇒ self-confirming by construction. **Recorded, not buried:** R also keeps `propose_edit` advertised in ASK mode, so a read-only turn can still push proposed prose — defensible, but a PRODUCT call; the tier is now the single place to change it. Enforcement is two-layer and each was mutation-tested separately: `UiToolDef._meta` is **required**, so a new `ui_*` tool cannot COMPILE untiered (TS2741 before any test runs); a spec covers the assembled list + the tier VALUES. The spec's first draft failed as a TS2339 pointing at the assertion — red, but the reader got a type complaint instead of the rule; it now reports `Expected "propose_edit=R", Received "propose_edit=undefined"`. LIVE: untiered **8 → 0**. |
| — | **Audit close-out — the classes that were NOT defects.** Of six defect classes swept over all 299 tools, two were real (A: enum-in-prose → K20; B: missing tier → K21) and four are closed as non-findings, recorded so nobody re-opens them: **D boolean-subschema = 0** (the federation guard holds); **C `$ref` = 8, all resolvable** — nested value objects after K16 flattened the top level, verified 0 dangling refs; **E writer-all-optional = 5, PATCH semantics** — `settings_update_profile` documents "only the provided fields change", and `glossary_adopt_standards` already flags the empty case (`"no genres or kinds specified — this will adopt nothing new"`, external audit #11), so a `{}` call is an explicit no-op, not a silent one; **F description-without-a-verb = 64, my regex was crude** (it lacked "Navigate", "Reorganize", "Soft-archive", …) — not a finding at all. |

| ~~K22~~ | **`tool_list` advertised the OPPOSITE of what the handler does — on the ONLY discovery surface.** `tool_list` exists twice: chat-service advertises the definition the model reads, ai-gateway's `handleToolList` executes it. chat-service said `include_deprecated` `default: True` / *"Default true."*; the handler applies **false**, deliberately and documented in place (spec 2026-07-22 review: *"a browsing agent should see the CURRENT surface, not the shrunk-away legacy tools as noise — the book catalog was 16 active + 19 deprecated"*). So the DEFINITION was wrong, not the handler. **Blast radius:** a model that trusts "default true", omits the arg, and looks for a tool it remembers by its OLD name is told the tool does not exist — right after a unification renamed tools en masse, i.e. exactly when that lookup is most likely, and it presents as a missing tool rather than a wrong default. Nothing red, because each side was internally consistent — the K10 shape again (the JSON contract file pins frontend-tool args only and never saw it). Same guard: read the TS source. Four checks, because the default lives in several places a reader trusts — advertised vs the **executing handler's fallback**, advertised vs ai-gateway's own tool def, the **prose** (a correct `default` under "Default true." is still a lie), and the arg set — plus a guard-the-guard that fails loudly if `find-tools.ts` moves. Mutation-verified. |
| — | **Two clean negatives + two probe bugs, recorded so they aren't re-run.** (1) **Undo targets: 24 declared, 0 dangling** — every `undo_hint` names a tool that exists, so no Undo button 404s. (2) **Tier-R composition liveness: 26 tools called, 0 crashes** — no traceback/500 leaking as an answer (weak signal: most correctly refuse on missing args). (3) **Tenancy audit: 3 composition tools declare `scope:"book"` while being USER-scoped** (`arc_template_list/create/archive` — all owner-scoped at the repo via `tc.user_id`, so **not** a tenancy hole). Notably `_meta.scope` is **validated everywhere and read by NO consumer** — Go panics on a bad scope, Python raises, and nothing in chat-service or ai-gateway ever reads it — so the misdeclaration is inert *and* undetectable by any test. Left as-is deliberately; recorded because if a consumer is ever added, these three are wrong. (4) **My own probe bugs**: `/tmp` in Windows Python ≠ Git-Bash `/tmp` (silently read an empty target list), and `tool_list` returns `{categories:{…}}` for `category="all"` but `{category,count,tools}` for a named one — my parser saw only the second and reported "catalog: 0". The second misparse is what sent me to read the definition and find K22. |

| ~~K23~~ | **The discovery pair could not discover 10 tools it serves — including ITSELF.** F17 retired `find_tools` on the stated grounds that top-K "structurally CANNOT surface a tool outside the K matches", whereas `tool_list`/`tool_load` "have no such blind spot". They had one. Live: **tools/list served 299, tool_list enumerated 289.** The missing 10 were the CONSUMER-LOCAL set — `tool_list`/`tool_load` themselves, the 7 `ui_*`, and `propose_edit` — because both handlers walked `federation.catalog()`, which holds only federated PROVIDER tools. **The sharp end:** `tool_load('ui_open_book')` → `not_found`, which does not mean "unfederated" — it tells the model the tool **does not exist**. And it bit hardest where least visible: `ui_*`/`propose_edit` are advertised CONDITIONALLY (F7c nav-intent / editor-surface gates), so on precisely the turn the gate withholds one, discovery was the only route back and it denied the tool was real. `handleListTools`' own comment already claimed the opposite intent (*"ai-gateway lists them so they are discoverable + validated at one seam"*) — the code did not honour its own comment. Fixed at the seam (both result builders) **plus a home**: `domainOf` bucketed them under `tool`/`ui`/`propose`, absent from GROUP_DIRECTORY *and* CATEGORY_ENUM, so even once listed no targeted category could reach them → new **`meta`** group + 3 aliases, mirrored in chat-service. **The existing drift-lock WORKED** — `find-tools.spec 'mirrors chat-service GROUP_DIRECTORY verbatim'` went red the moment I added a key to one side. Mutation-verified (reverting reds 3 of 4; the 4th pins federated listing undisturbed). LIVE: **289 → 299, missing []**; `category=meta` → 10 named; `tool_load` `not_found: none`. |

| K24 | **OPEN QUESTION for the human — a genuine SPEC TENSION, not a defect. I tried to "fix" it and was wrong.** Measured live: **17 of 17 tools with a `detail` selector default to `"full"`** — none to `summary`. `docs/standards/mcp-tool-io.md` **OUT-2** says plainly *"Default to the smaller shape."* Cost is real and concentrated: `jobs_list` with NO arguments returns **45,599 B** (`detail=summary` → 21,730 B, a **52% / ~6k-token** cut on one call), and it trips the kit's own size warning by 6.5× on the DEFAULT path — `"mcp tool result is large … tool=jobs_list bytes=52351 (warn>8000)"`. So the warning fires, on every such call, and nothing acts on it. **But `full` is SEALED, not drifted.** The Context Budget Law's own T1 row specifies *"versioned default (compiler passes `summary`, **federated keep `full`**)"* with success criteria *"federated caller still gets `full`"* / *"**zero consumer regression**"*. I flipped `jobs_list` to `summary`; `test_jobs_list_shape_and_owner_scope` reddened with the tripwire comment `# versioned-default migration (spec §6b)`, I read the spec, and **reverted** (jobs-service back to 98 pass). **The tension worth deciding:** OUT-2 protects the context-constrained caller, T1 protects federated consumers from regression — and **the AGENT is the federated caller**, so the party the Context Budget Law exists to protect is precisely the one deliberately handed the heavy shape. Either OUT-2 needs a "except already-federated tools during the T1 migration" carve-out, or the migration's federated half is now due. **A product call on a sealed decision — not mine to make unilaterally.** |
| — | **Output-side sweep: 2 more negatives.** (1) **The audit-#9 payload dedup is inert on the Python services — as its own docstring predicted, and harmlessly.** `patch_tool_run_size_gate`'s note says the `convert_result` path is "a DEAD END … every MCP tool is annotated `-> dict`, so `output_schema` stays None" and adds "(the same is true of the audit-#9 dedup on that path — separate follow-up)". Verified live: Go tools return a 46-byte placeholder + full `structuredContent` (dedup working); Python tools (`kg_project_list`, `jobs_list`) return the payload in **text only, with NO `structuredContent`** — so there is **no duplication to remove**, and the inert patch costs nothing. Consumers are fine (chat-service reads `structuredContent` first, then falls back to parsing text). Recorded so the "separate follow-up" is not re-opened as a token-waste bug it is not. (2) **The result-size gate WORKS** — it warned on `jobs_list` exactly as designed; the ceiling (512 KB hard / 8 KB warn) is simply far above the 52 KB seen. |

| ~~K25~~ | **Two tools silently truncated their lists (OUT-5) — FIXED + tested.** Probing every Tier-R `limit`-bearing tool against >1 row: `world_list` (capped default 20, returned only `worlds` — 27 worlds read as "you have 20") and `composition_arc_template_list` (capped, returned only `arc_templates`+`scope` — 31 read as `limit`). Both are READS a browsing agent uses to answer "what do I have", so the wrong count flows into its next decision. **world_list (Go)** now reuses book-service's own `listPage` envelope (`total/returned/next_offset/has_more/is_complete` + prose) via one cheap owner COUNT — LIVE *"1 of 27 worlds so far. Call `world_list offset=1` …"*. **arc_template_list (Python)**: repo hard-bounds in SQL so `total==returned` would still lie → uses the **fetch-`limit+1`** trick `kg_project_list` documents, returning `{returned, more, guidance}` — LIVE *"showing 1 — more exist…"*. Both mutation-verified. **The other 10 limit-bearing Tier-R tools are compliant** — each carries a flag (`is_complete`/`truncated`/`more`/`has_more`/`window_available`/`total`) or had an empty fixture. |

| K28 | **THE REAL BUG behind K27 (answered the user's "(b): does CI run these?"). NO Python test suite runs in CI — AT ALL.** All 10 GitHub Actions workflows run **`go test` + `cargo test` only**; a repo-wide grep finds **zero `pytest` invocations**. `domain-db-smoke.yml` (built 2026-07-11 for this exact "green by skip" rot) wires throwaway Postgres for **book + glossary only — both Go**. `TEST_COMPOSITION_DB_URL` / `TEST_KNOWLEDGE_DB_URL` / `TEST_NEO4J_URI` are set in **0 workflows**. The pre-commit hook runs **static gates only** (provider/db-safety/tier/no-abs-path/i18n — explicitly "no suite execution"). No other CI system exists (no gitlab/circle/jenkins/azure). **⇒ chat 1906 · knowledge 4075 · composition 2387 · translation/jobs/lore-enrichment — thousands of Python tests — gate NOTHING automatically.** They pass only when a dev runs `pytest` by hand. The K27 integration ROT is a *symptom*: those suites drifted from the schema precisely because nothing ever ran them. Even the green Python UNIT tests are ungated — a future break merges clean. **Caveat on the claim:** scoped to the workflow configs on this branch; I cannot see org-level required workflows or other-branch CI. **Remediation (2 layers):** (1) a `python-tests` CI job (mirror domain-db-smoke: ephemeral PG + a Neo4j service container, set the 3 env vars, run each service's pytest) — an INFRA change the user owns, and it can't turn on green until (2) the K27 rot is repaired. So the order is **fix the rot (K27/a) → then wire CI → then it gates**. |
| K27 | **SKIP-BY-DEFAULT SUITE ROT — real, pre-existing, SEPARATE TRACK. Surfaced by the user asking "why so many skips, are they errors?"** The skips themselves are NOT errors: they are DB-gated integration tests (need a throwaway Postgres via `TEST_*_DB_URL` / Neo4j via `TEST_NEO4J_URI`) + a 7-test opt-in eval — skipping when the env is unset is by design. **But when actually RUN against throwaway DBs, they are partly ROTTED** — the exact `feedback_skip_by_default_suites_rot` failure mode: nobody runs them, so they drift from the schema and nobody notices. **composition: 8/340 fail** (proven on a PRISTINE migrated DB, so not dirty-DB); dominant cluster = **5× `outline_node_kind_check`** — fixtures insert `kind='arc'` outline_nodes, illegal since the arc→structure_node split (the run log even says *"arc lift APPLIED — arcs lifted to structure_node, arc rows DELETED"*). **knowledge: 16/342 Postgres fail** (against the shared test DB — some may be dirty-DB, not all verified pristine); **270 Neo4j tests entirely unverified** (no `TEST_NEO4J_URI` in this probe). **Confirmed NOT mine:** my commits touch none of these paths and the composition cluster reproduces on a pristine schema. **Scope:** ~24 proven-rotted PG tests + 270 unverified Neo4j — a schema-archaeology track (defer-gate #2, large/structural), orthogonal to the tool-catalog migration. **Deeper systemic gap:** these never gate anything locally (skip without the env) — if CI does not run them either, the whole `tests/integration/db` tree is dead weight. Needs: confirm CI executes them, then repair the fixtures worst-first. **Decision pending: fix now vs track + continue the MCP deep-dive.** |

| K28·1 | **DONE — `python-unit-tests.yml` ships (this branch).** Matrix over the 6 verified-green Python services (chat/knowledge/composition/translation/jobs/lore-enrichment); `pip install ./sdks/python` + reqs; Python 3.12; SERIAL (deterministic — dodges the lore-enrichment xdist flake); integration/db skips (no DB env). Recipe verified locally (SDK imports pip-installed, all 6 unit suites green). Can't run GH Actions from here → first push is the E2E proof; additive + path-filtered so it touches no existing CI. **Remaining K28 steps:** (2) repair integration rot [composition ✓ below]; (3) a `python-integration-tests` job (ephemeral PG + Neo4j container + the 3 env vars). Also add the 4 unverified Python services (campaign/learning/video-gen/worker-ai) once each is confirmed green. |
| K27·comp | **DONE — composition DB-integration rot triaged; ALL 8 failures are STALE tests, ZERO live bugs.** Verified each against current code: 4× `arc_lift` = obsolete rehearsal of the shipped M4/M5 migration (run_migrations has no target-version, so the pre-M5 `kind='arc'` shape is no longer constructible); `scene_requires_chapter_id` = the CHECK was intentionally relaxed (outline-before-draft); `package_rekey beat rows` = `kind='beat'` removed from outline_node; `c16 pending_work` = create_node moved from work_id- to project_id-addressed; `repositories compile_pipeline` = harness gap (global get_pool never init'd). Each now a **skip WITH a specific reason** naming what changed + how to restore — silent rot → documented. Suite now **332 pass / 8 reasoned skip / 0 fail** with a DB; unit run stays 2387. Proper fix (update each to current behavior) is follow-up. **STILL OPEN in K27:** knowledge **16 PG failures unclassified** + **270 Neo4j tests unverified** — same triage owed. |

| K27·know | **DONE — knowledge triaged; found + fixed a REAL PRODUCTION BUG plus restored 16 tests.** Ran the never-run knowledge integration suite (PG on a pristine throwaway; Neo4j against the shared dev graph after a safety-scan confirmed all DETACH DELETEs are user_id-scoped). **PG (16 fail → 0):** the `list_all_for_user` history cursor passed a nullable `cur_completed_at` whose first use was `$N IS NOT NULL` — Postgres can't infer the type at PREPARE, so asyncpg raised `AmbiguousParameterError` on **every 2nd page of extraction-job history** (a live 500, mocked-away in unit tests, never run in CI). Fixed with `::timestamptz` casts. 12× context_build RESTORED (harness: the conftest `pool` fixture now also sets the global `_knowledge_pool` the real router calls). c12a rewritten to respect K16.3 (one-active-per-project). **Neo4j (613/618 pass — mostly HEALTHY):** 3× entities_browse fixed (added the new `expected_version` optimistic-concurrency arg); the `neo4j_driver` fixture now also sets the global driver + the mis-gated `_live` test requests it. **2 documented skips** (flagged, not silently passed): a deterministic Passage.id collision (id-scheme investigation owed) and propose_edge's park-to-triage → reject-unknown-endpoints behavior change (**needs domain confirmation — possible regression**). Full integration suite now **616 pass / 2 skip / 0 fail**; unit 3957. **K27 remaining:** the flagged pair; and the 4 untouched Python services' suites are unit-only in CI so far. |
| K27·edge | **RESOLVED — `kg_propose_edge` did NOT lose park-to-triage. NOT a regression; the test was stale.** Answered the flag I raised in K27·know, and my "possible regression" wording was the near-miss: I inferred it from a red test without reading the handler. The code disproves it in one look — `_handle_kg_propose_edge` still ends in `ctx.triage_repo.park(...)` → `{"parked": True}` and still never WRITES Neo4j (INV-K1 intact). What changed is a **fail-fast precheck added IN FRONT of the park**: WS-4B (`70fc8b549`, **2026-07-09**) rejects an edge whose endpoints aren't `:Entity` nodes with `KG_ENDPOINT_NOT_NODE`, because the confirm-time write matches endpoints by `Entity.id` and would otherwise park then dead-end two steps later at confirm. It READS Neo4j — INV-K1 is about not writing it. Deliberate, documented, and already unit-covered (`test_propose_edge_fails_fast_when_endpoint_not_a_node` asserts `park.assert_not_awaited()`, with a sibling test proving the park path still works via `_patch_endpoints_present`). The integration test simply **predated the gate** and used phantom ids `"ent-a"/"ent-b"` that could never be nodes, so it asserted a path its own inputs made unreachable. **Fix: un-skipped and restored** — it now seeds two real endpoints via `merge_entity` and exercises the true park→triage-list→resolve flow (cleanup `DETACH DELETE` scoped to the test's fresh `uuid4` owner). **Added** `test_propose_edge_rejects_endpoints_that_are_not_nodes_live` so the precheck is proven against real Cypher, not just a stubbed `existing_entity_node_ids`. **Mutation-verified both ways:** deleting the precheck reds ONLY the new fail-fast test (and confirms the old test's premise — without the gate, phantom ids really do park); disabling the `park(` call reds ONLY the restored park test. Zero production-code change (`git diff app/` = 0 lines). Knowledge integration **624 pass / 1 skip / 0 fail** (was 622/2 at HEAD; 624→625 collected). **K27 remaining: just the Passage.id collision skip.** |
| K27·psg | **DONE — K27 CLOSED. The Passage.id collision was a never-executed test; fixing it exposed a REAL mock-only coverage hole on the canon gate.** The collision was NOT an id-scheme bug: `passage_canonical_id` is sound. The test built its "legacy" node by cloning the draft (`CREATE (l:Passage) SET l = properties(p), l.id = 'legacy-null-canon', …`) — `SET l = properties(p)` copies `p.id` FIRST, tripping `passage_id_unique` before the later `l.id = …` can rescue it. It could never have passed against a real graph; it shipped with the feature (`55b9eba25`) and was never run. Rewrote the setup to write a genuine passage and `REMOVE` its `canon` flag (also a truer model of "predates the flag"). With the setup fixed the assertions pass — **the canon gate itself is correct, no production bug.** **The real find:** that same gate (`$include_drafts OR coalesce(node.canon, true) = true`) also lives in **`find_passages_by_fulltext`**, where it had ONLY mock wiring coverage. Proof: **deleting the fulltext canon gate entirely — which leaks unpublished drafts into `surface=canon` reads — passes all 20 pre-existing unit tests**; only the new `test_find_passages_by_fulltext_canon_filter` catches it. Added that, plus `test_delete_passages_for_source_canon_scoping_keeps_both` for the D-R20 keep-both reap (data-loss shaped: a draft index must never wipe published canon). Mutation-verified: flipped-coalesce and gate-removal each red the right test. Zero production-code change (`git diff app/` = 0 lines). Knowledge integration **627 pass / 0 skip / 0 fail** (K27's last skip cleared); unit 3957. |
| K29 | **DATA-LOSS BUG FIXED — a test fixture could DROP the real `loreweave_lore_enrichment` DB, and did so in a decoy repro. Found by extending the K27 rot-triage to the 2 remaining never-triaged DB suites.** `services/lore-enrichment-service/tests/db/conftest.py` read `TEST_LORE_ENRICHMENT_DB_URL` **or fell back to the PRODUCTION `LORE_ENRICHMENT_DB_URL`**, then ran `run_down_migrations()` (DROP TABLE ×12) at the top of every test — and had **no `_guard_throwaway`**, despite a docstring claiming it "mirrors knowledge-service" (which has both). The root conftest's `os.environ.setdefault("LORE_ENRICHMENT_DB_URL", <placeholder>)` does NOT protect: `setdefault` is a no-op when the var is already exported, which it IS in any compose/dev shell and in-container. **Proven, not theorised:** created a production-shaped decoy `loreweave_lore_enrichment_demo`, seeded a sentinel row, ran `pytest tests/db` with only the prod var set → **39 tests PASSED green while all 12 tables were dropped and the row destroyed.** Exactly the class CLAUDE.md's "Destructive DB ops in tests" documents (the unscoped-DELETE-wiped-every-user's-books incident). **3-layer fix, matching the rule's own prescription:** (1) conftest now reads the dedicated TEST var ONLY (no prod fallback) + a `_guard_throwaway()` that REFUSES a non-marker DB name before the first destructive statement + a `guarded-dir` pragma + a history-warning docstring; (2) extended `scripts/db-safety-gate.py` with a `production-DSN-read-in-test` check (a test conftest / schema-reset fixture reading a non-TEST `*_DB_URL`/`*_DATABASE_URL`/`*_DSN` is now a finding) — deliberately NARROW (fires only where a wipe is reachable: conftest or a down-migration/drop helper) so it never cries wolf on the sanctioned scoped-delete `*_db.py` tests. **Re-ran both attack vectors against the fix:** prod-var-only → 39 SKIP (no TEST var); TEST-var-at-non-throwaway → 39 ERROR (guard refuses); sentinel row SURVIVED both; a properly-named throwaway still runs 39 pass. Gate: clean on the fixed tree (EXIT 0), catches the original at line 25. Lore-enrichment full suite now green on a throwaway. |


## Drift log (near-misses — an empty log here would be dishonest)
- **Shipped a vacuous assertion into my own keep-both test, and mutation testing caught it.**
  `test_delete_passages_for_source_canon_scoping_keeps_both` asserted "the legacy null-canon
  passage survives a canon=False reap" — but I parked the legacy node under its OWN `source_id`,
  which the reap never matched on `source_id` anyway. The assertion was green for the wrong
  reason, and the flipped-`coalesce` mutation walked straight past it. Caught only because I
  mutated the legacy default separately instead of stopping at the coarse gate-removal mutation.
  Lesson: a "X survives" assertion proves nothing unless X is inside the operation's blast
  radius — and one mutation per clause, not one per function, is what surfaces that.
- **Called `kg_propose_edge` a "possible regression" from a red test alone.** I wrote the K27·know
  skip reason ("park-to-triage → reject … may be a real regression") without opening the handler.
  One read disproved it: the `park(` call is still there, and the reject is a precheck deliberately
  added IN FRONT of it (WS-4B, 2026-07-09) with its own unit test. The stale-test explanation was
  the boring, correct one. A skip reason that speculates about production is a claim — it needs the
  same root-cause bar as a fix, and mine escaped with a hedge instead. Cost: a scary flag carried
  across a compaction boundary. (Same shape as `feedback_verify_explore_rootcause_before_fixing`,
  applied to my own triage note.)
- **Plan F3 was wrong.** "Fail the healthcheck on PARTIAL" would have deadlocked: `glossary-service`
  declares `depends_on: ai-gateway: service_healthy`. Caught by checking `depends_on` before writing.
- **First federation-guard walker had a false positive.** Flagged 12 boolean `default:` values as
  subschemas; shipped as written it would have panicked services at boot — worse than the bug.
  Caught by scanning all 10 live providers before wiring it in.
- **Claimed "the skill fix will change S00b behavior."** It could not: S00b runs `enabled_skills: []`.
  Corrected after measuring, not before.
- **Claimed the duplicate calls were "all parallel at iteration 0."** Wrong for session 019f8dda —
  those were sequential RETRIES (iterations 1,2,3) with a different root cause (`created:0` reading as
  failure). Collapsing them would have been incorrect. Caught by querying the `iteration` field instead
  of trusting the earlier reading.
- **Claimed ai-gateway "degrades silently."** Too coarse — the signal existed and was plumbed; it was
  wired only to the F17-retired path. Corrected after reading the code.
